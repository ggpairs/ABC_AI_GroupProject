# "我的"页面功能说明

## 页面概览
"我的"页面是用户的个人中心，展示用户信息、收藏内容，并提供联系方式和版本信息。

## 功能模块详解

### 1. 用户信息区域（顶部渐变背景）

#### 用户头像和基本信息
- **头像显示**：
  - 微信登录用户：自动获取微信头像（存储在profile.avatar_url）
  - 用户名登录用户：显示默认占位头像
  - 头像样式：圆形，带白色半透明边框
  
- **用户名显示**：
  - 优先显示：profile.username（用户名登录）
  - 其次显示：user.email（邮箱）
  - 默认显示：'未登录'
  
- **角色标签**：
  - 管理员：显示"管理员"标签
  - 普通用户：显示"普通用户"标签
  - 样式：白色半透明背景，圆角标签

#### 用户统计卡片
- **收藏案例数量**：显示用户收藏的案例总数
- **收藏故事数量**：显示用户收藏的志愿者故事总数
- 样式：两列网格布局，白色半透明背景，毛玻璃效果

### 2. 我的收藏

#### 收藏分类Tab
- **案例Tab**：显示收藏的案例列表及数量
- **故事Tab**：显示收藏的志愿者故事列表及数量
- 交互：点击切换Tab，高亮显示当前选中Tab

#### 收藏列表
- **案例列表**：
  - 使用CaseCard组件展示
  - 点击卡片进入案例详情页
  - 支持在详情页取消收藏
  
- **故事列表**：
  - 使用StoryCard组件展示
  - 点击卡片进入志愿者故事详情页
  - 支持在详情页取消收藏

#### 空状态处理
- 无收藏案例时：显示空状态提示，提供"浏览案例"按钮跳转到案例页
- 无收藏故事时：显示空状态提示，提供"浏览故事"按钮跳转到志愿者页

### 3. 关于我们

#### 功能说明
- **点击触发**：弹出模态框显示分社介绍
- **内容包含**：
  - 分社名称：ABC数字创造营分社
  - 使命愿景：用数字技术创造价值
  - 详细介绍：数字化转型赋能说明
  - 联系方式：
    - 邮箱：contact@abc-digital.com
    - 电话：400-123-4567
  - 版本信息：v1.0.0

#### 交互选项
- **关闭按钮**：关闭模态框
- **返回首页按钮**：点击后跳转到首页

### 4. 联系我们

#### 功能说明
- **点击触发**：弹出操作菜单
- **操作选项**：
  1. **拨打电话**：
     - 调用系统拨号功能
     - 电话号码：400-123-4567
     - 失败提示：显示"拨号失败"
  
  2. **复制邮箱**：
     - 复制邮箱地址到剪贴板
     - 邮箱：contact@abc-digital.com
     - 成功提示：显示"邮箱已复制"
  
  3. **返回首页**：
     - 跳转到首页Tab

### 5. 版本信息

#### 功能说明
- **静态展示**：不可点击
- **显示内容**：
  - 左侧：版本信息图标 + "版本信息"文字
  - 右侧：当前版本号 v1.0.0
- **样式**：卡片样式，与其他功能按钮保持一致

### 6. 退出登录

#### 功能说明
- **点击触发**：弹出确认对话框
- **确认对话框**：
  - 标题：确认退出
  - 内容：确定要退出登录吗？
  - 取消按钮：关闭对话框
  - 确认按钮：执行退出登录

#### 退出流程
1. 用户点击"退出登录"按钮
2. 显示确认对话框
3. 用户确认后：
   - 调用signOut()方法清除登录状态
   - 显示"已退出登录"提示
   - 自动跳转到登录页面（由RouteGuard处理）

## 页面权限

- **访问权限**：需要登录（使用withRouteGuard包裹）
- **未登录访问**：自动跳转到登录页面
- **登录后访问**：显示完整功能

## 数据刷新机制

- **页面显示时**：使用useDidShow钩子自动刷新收藏数据
- **组件挂载时**：使用useEffect钩子加载收藏数据
- **收藏状态变化**：在详情页取消收藏后，返回"我的"页面会自动刷新

## 视觉设计

### 配色方案
- **顶部背景**：主色渐变（蓝色到青色）
- **统计卡片**：白色半透明背景，毛玻璃效果
- **功能按钮**：白色卡片背景，带阴影
- **图标颜色**：
  - 关于我们：主色（蓝色）
  - 联系我们：次要色（青色）
  - 版本信息：灰色
  - 退出登录：红色

### 布局特点
- **响应式设计**：适配不同屏幕尺寸
- **卡片式布局**：功能模块清晰分离
- **统一间距**：所有元素使用一致的间距系统
- **视觉层次**：通过颜色、大小、阴影建立清晰的视觉层次

## 用户体验优化

1. **即时反馈**：所有操作都有Toast提示
2. **确认机制**：重要操作（退出登录）需要二次确认
3. **空状态引导**：无收藏时提供明确的引导按钮
4. **数据统计**：直观展示用户的收藏数量
5. **便捷操作**：联系方式支持一键拨号和复制
6. **流畅导航**：支持快速返回首页

## 技术实现要点

- **状态管理**：使用React Hooks管理组件状态
- **数据获取**：通过getUserFavorites API获取收藏数据
- **权限控制**：使用withRouteGuard HOC保护页面
- **类型安全**：使用TypeScript确保类型安全
- **性能优化**：使用useCallback避免不必要的重渲染
